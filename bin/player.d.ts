import { Card } from "./card";
export declare type Player = {
    bank: number;
};
export declare type Hand = {
    bet: number;
    cards: Card[];
};
export declare type Dealer = {
    cards: Card[];
};
